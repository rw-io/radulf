`mergeBranch` (src/server/git.ts) now recovers from a delivery worker that died mid-merge in the shared parent checkout: a stale `MERGE_HEAD` pointing at the run branch is aborted and the merge redone; a `MERGE_HEAD` from anywhere else is refused with an error naming the repo path and saying a merge started outside Radulf must be finished or aborted there; and a run branch already contained in base is reported as `{ ok: true, alreadyMerged: true }` with the original merge commit (no new commit, `onCommitted` skipped), which `ReviewService.deliver` forwards into the `review.decided` payload. The dirty-checkout error now names the repo path and tells the operator to commit/stash and press Retry merge, and the stale reaper's Needs Attention reason explains that Retry merge will abort or record the half-finished merge. Real-repo, reaper and reviewService tests cover each path.

Verdict this round: **revise** — one acceptance criterion fails because the final task hoisted the old bare string `"target checkout has uncommitted changes"` into an unused exported constant; the fix is a two-line inline in `src/server/git.ts`.
