Retry merge now recovers from a delivery worker that died mid-merge in the shared parent checkout. `mergeBranch` checks for an in-progress merge first: if `MERGE_HEAD` is the run branch it aborts Radulf's own half-finished merge and merges cleanly; any other `MERGE_HEAD` is refused with an error naming the repo path and saying a merge started outside Radulf must be finished or aborted there. If the run branch is already contained in base (the worker committed but died before the DB write), it returns `alreadyMerged: true` with the existing merge commit, fires no ref write, and `ReviewService.deliver` surfaces that flag in the `review.decided` activity payload. The dirty-checkout error now names the repo path and tells the operator to commit or stash and press Retry merge, and the stale reaper's Needs Attention reason says the same. Covered by new real-repo cases in `git.test.ts` plus reaper and reviewService tests; `make check` passes. This attempt only removed the dead `DIRTY_CHECKOUT_ERROR` constant and inlined the message, as the previous review requested.
